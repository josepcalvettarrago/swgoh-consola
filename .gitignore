node_modules/
web/dist/
.dev.vars
*.local
.DS_Store
.wrangler/

# Service account de Firebase — NUNCA subir (contiene la clave privada de admin)
firebase/*adminsdk*.json
firebase/*service*account*.json
*serviceAccount*.json
# Logs de la ingesta local
*.log

# Fase 5.4 — datos maestros y rescate del admin (contienen procedimiento sensible / tocan Firestore)
/ADMIN.md
/scripts/admin-reset-local.mjs

name = "swgoh-consola"
main = "src/index.js"
compatibility_date = "2025-01-01"

# Sin cron: la ingesta corre en GitHub Actions (el egress del Worker a swgoh.gg está
# bloqueado por el managed challenge de Cloudflare). Este Worker solo LEE de Firestore.

# Variables NO secretas.
[vars]
FIRESTORE_DB = "swgohapi"
ALLY_CODE    = "355463284"
GUILD_ID     = "U6tWH0WuSDyl_g7lmgZm-w"
PAGES_ORIGIN = "*"            # en prod: https://<tu-proyecto>.pages.dev

# Secret (NUNCA en el repo). Solo lectura de Firestore:
#   wrangler secret put FIREBASE_SERVICE_ACCOUNT   # JSON del service account en una línea
# En local: usar .dev.vars (ya está en .gitignore).

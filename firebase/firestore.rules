rules_version = '2';

// Fase 0: deny-all por defecto. Se abrirá con Firebase Auth en la Fase 5.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
